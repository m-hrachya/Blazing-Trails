namespace BlazingTrails.Shared;

public static class Constants
{
    public const string SecureAPIClient = nameof(SecureAPIClient);

    public static class Roles
    { 
        public const string Admin = nameof(Admin);
    }
}
